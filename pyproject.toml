[project]
name = "project-dvul"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "streamlit",
    "langchain",
    "requests"
]

[tool.ruff]
line-length = 88
