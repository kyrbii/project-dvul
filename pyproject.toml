[project]
name = "project-dvul"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "streamlit",
    "langchain",
    "requests",
    "langchain-nvidia-ai-endpoints>=1.2.1",
    "dotenv>=0.9.9",
]

[tool.ruff]
line-length = 88
